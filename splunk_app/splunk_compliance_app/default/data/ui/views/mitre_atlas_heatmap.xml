<dashboard version="1.1">
  <label>MITRE ATLAS Matrix Heatmap</label>
  <description>ATT&amp;CK-style coverage matrix for MITRE ATLAS tactics and techniques. Shows GAP / OBSERVED / DETECTED per cell, tactic coverage %, and drilldown hunts. Run OrchestraACME Workshop → Deep Workshop to populate all lab-executable techniques. Reference: https://atlas.mitre.org/</description>

  <fieldset submitButton="true" autoRun="true">
    <input type="time" token="time_range" searchWhenChanged="false">
      <label>Time Range</label>
      <default>
        <earliest>-7d</earliest>
        <latest>now</latest>
      </default>
    </input>
    <input type="dropdown" token="status_filter" searchWhenChanged="false">
      <label>Status Filter</label>
      <choice value="*">All</choice>
      <choice value="GAP">Gaps only (not observed)</choice>
      <choice value="OBSERVED">Observed only</choice>
      <choice value="DETECTED">Detected / blocked only</choice>
      <default>*</default>
    </input>
  </fieldset>

  <!-- KPI row -->
  <row>
    <panel>
      <single>
        <title>Lab Catalog Coverage %</title>
        <search>
          <query>| inputlookup acme_framework_lookup | stats count as expected
| appendcols [ search `acme_genai_index` earliest=$time_range.earliest$ latest=$time_range.latest$
    | stats dc(technique_id) as observed ]
| eval coverage_pct=round(100*observed/expected,1) | fields coverage_pct</query>
        </search>
        <option name="drilldown">none</option>
        <option name="colorBy">value</option>
        <option name="colorMode">block</option>
        <option name="rangeColors">["0xdc4e41","0xf8be34","0x53a051"]</option>
        <option name="rangeValues">[25,50]</option>
        <option name="useColors">1</option>
      </single>
    </panel>
    <panel>
      <single>
        <title>Tactics With Evidence</title>
        <search>
          <query>`acme_genai_index` earliest=$time_range.earliest$ latest=$time_range.latest$ technique_id=AML.*
| lookup acme_framework_lookup technique_id OUTPUT tactic_id
| stats dc(tactic_id) as count</query>
        </search>
        <option name="drilldown">none</option>
      </single>
    </panel>
    <panel>
      <single>
        <title>Techniques Detected / Blocked</title>
        <search>
          <query>`acme_genai_index` earliest=$time_range.earliest$ latest=$time_range.latest$ technique_id=AML.*
(defenseclaw_blocked=true OR codeguard_blocked=true OR workflow.blocked=true)
| stats dc(technique_id) as count</query>
        </search>
        <option name="drilldown">none</option>
      </single>
    </panel>
    <panel>
      <single>
        <title>Gap Count (Not Observed)</title>
        <search>
          <query>| inputlookup acme_framework_lookup | fields technique_id
| join type=left technique_id [
    search `acme_genai_index` earliest=$time_range.earliest$ latest=$time_range.latest$
    | stats count as events by technique_id
  ]
| where isnull(events) OR events=0
| stats count as gaps</query>
        </search>
        <option name="drilldown">none</option>
        <option name="colorBy">value</option>
        <option name="colorMode">block</option>
        <option name="rangeColors">["0x53a051","0xf8be34","0xdc4e41"]</option>
        <option name="rangeValues">[5,15]</option>
        <option name="useColors">1</option>
      </single>
    </panel>
  </row>

  <!-- Tactic coverage strip (like ATT&CK heatmap tactic row) -->
  <row>
    <panel>
      <title>MITRE ATLAS Tactic Coverage (16 tactics)</title>
      <table>
        <search>
          <query>| inputlookup acme_framework_lookup
| stats count as expected by tactic_id tactic_name
| join type=left tactic_id [
    search `acme_genai_index` earliest=$time_range.earliest$ latest=$time_range.latest$ technique_id=AML.*
    | lookup acme_framework_lookup technique_id OUTPUT tactic_id
    | stats dc(technique_id) as observed by tactic_id
  ]
| join type=left tactic_id [
    search `acme_genai_index` earliest=$time_range.earliest$ latest=$time_range.latest$ technique_id=AML.*
      (defenseclaw_blocked=true OR codeguard_blocked=true OR workflow.blocked=true)
    | lookup acme_framework_lookup technique_id OUTPUT tactic_id
    | stats dc(technique_id) as detected by tactic_id
  ]
| lookup acme_atlas_tactics_lookup tactic_id OUTPUT tactic_order
| eval observed=coalesce(observed,0), detected=coalesce(detected,0)
| eval coverage_pct=if(expected&gt;0,round(100*observed/expected,0),0)
| eval tactic_bar=case(coverage_pct=0,"░░░░░░░░░░",coverage_pct&lt;25,"█░░░░░░░░░",coverage_pct&lt;50,"██░░░░░░░░",coverage_pct&lt;75,"█████░░░░░",coverage_pct&lt;100,"███████░░░",1=1,"██████████")
| sort tactic_order
| table tactic_order tactic_name tactic_id expected observed detected coverage_pct tactic_bar
| rename tactic_order AS "#", tactic_name AS "Tactic", tactic_id AS "ID", expected AS "In Catalog", observed AS "Observed", detected AS "Blocked/Detected", coverage_pct AS "Coverage %", tactic_bar AS "Heat"</query>
        </search>
        <option name="drilldown">none</option>
        <option name="wrap">false</option>
        <format type="color" field="Coverage %">
          <colorPalette type="minMidMax" minColor="#dc4e41" midColor="#f8be34" maxColor="#53a051"/>
          <scale type="minMidMax" minValue="0" midValue="50" maxValue="100"/>
        </format>
      </table>
    </panel>
  </row>

  <!-- Matrix heatmap chart -->
  <row>
    <panel>
      <chart>
        <title>Technique × Tactic heatmap — GAP (0), OBSERVED (1), DETECTED (2)</title>
        <search>
          <query>| inputlookup acme_framework_lookup
| lookup acme_atlas_tactics_lookup tactic_id OUTPUT tactic_order
| join type=left technique_id [
    search `acme_genai_index` earliest=$time_range.earliest$ latest=$time_range.latest$
    | stats count as events,
        sum(eval(if(defenseclaw_blocked=true OR codeguard_blocked=true OR workflow.blocked=true,1,0))) as blocks
      by technique_id
  ]
| eval heat_score=case(isnull(events) OR events=0,0, blocks&gt;0,2, events&gt;0,1, 1=1,0)
| eval short_name=technique_id." ".substr(technique_name,1,28)
| sort tactic_order technique_id
| chart max(heat_score) over short_name by tactic_name</query>
        </search>
        <option name="charting.chart">heatmap</option>
        <option name="charting.drilldown">none</option>
        <option name="charting.axisTitleX.text">Technique</option>
        <option name="charting.axisTitleY.text">Tactic</option>
        <option name="charting.legend.placement">top</option>
        <option name="charting.seriesColors">["0x5c6773","0x006d9c","0xdc4e41"]</option>
      </chart>
    </panel>
  </row>

  <!-- Full matrix table with drilldown (practical hunting) -->
  <row>
    <panel>
      <title>ATLAS Technique Matrix — full catalog with hunt drilldown</title>
      <table>
        <search>
          <query>| inputlookup acme_framework_lookup
| lookup acme_atlas_tactics_lookup tactic_id OUTPUT tactic_order
| join type=left technique_id [
    search `acme_genai_index` earliest=$time_range.earliest$ latest=$time_range.latest$
    | stats count as events, dc(incident_id) as incidents, latest(_time) as last_seen,
        sum(eval(if(defenseclaw_blocked=true OR codeguard_blocked=true OR workflow.blocked=true,1,0))) as blocks
      by technique_id
  ]
| lookup acme_technique_playbooks_lookup technique_id OUTPUT execution_mode is_top_10 threat_hunt_spl
| eval events=coalesce(events,0), blocks=coalesce(blocks,0)
| eval matrix_status=case(events=0,"GAP", blocks&gt;0,"DETECTED", events&gt;0,"OBSERVED", 1=1,"GAP")
| eval lab_action=case(is_top_10="true","Attack Panel → Top 10 Scenario", execution_mode="LIVE","Attack Panel → All 45 → EXECUTE", execution_mode="HYBRID","Attack Panel → All 45 → EXECUTE", 1=1,"Workshop → RUN ALL 45")
| where if("$status_filter$"="*",1,matrix_status="$status_filter$")
| sort tactic_order technique_id
| table tactic_name technique_id technique_name matrix_status events blocks incidents severity cvss_score execution_mode is_top_10 lab_action last_seen
| rename tactic_name AS "Tactic", technique_id AS "TechniqueID", technique_name AS "Technique",
    matrix_status AS "Status", events AS "Events", blocks AS "Blocks", incidents AS "Incidents",
    severity AS "Severity", cvss_score AS "CVSS", execution_mode AS "Lab Mode",
    is_top_10 AS "Top-10", lab_action AS "Light Up In Lab", last_seen AS "Last Seen"</query>
        </search>
        <option name="drilldown">row</option>
        <option name="wrap">true</option>
        <option name="rowNumbers">true</option>
        <format type="color" field="Status">
          <colorPalette type="map">{"GAP":#5c6773,"OBSERVED":#006d9c,"DETECTED":#dc4e41}</colorPalette>
        </format>
        <format type="color" field="Severity">
          <colorPalette type="map">{"Critical":#dc4e41,"High":#f1813f,"Medium":#f8be34,"Low":#53a051}</colorPalette>
        </format>
        <drilldown>
          <link target="_blank">/app/acme_genai_compliance/search?q=`acme_genai_index`%20earliest=$time_range.earliest$%20latest=$time_range.latest$%20technique_id=$row.TechniqueID$%20%7C%20sort%20-_time</link>
        </drilldown>
      </table>
    </panel>
  </row>

  <!-- Detection readiness (practical vs Splunkbase 8527) -->
  <row>
    <panel>
      <title>Detection Readiness — field tiers for your index</title>
      <table>
        <search>
          <query>`acme_genai_index` earliest=$time_range.earliest$ latest=$time_range.latest$ | head 5000
| eval has_tier1=if(isnotnull(gen_ai.usage.input_tokens) OR isnotnull(technique_id) OR isnotnull(incident_id),1,0)
| eval has_tier2=if(isnotnull(gen_ai.input.preview) OR isnotnull(gen_ai.output.preview) OR isnotnull(defenseclaw.action),1,0)
| eval has_workflow=if(isnotnull(workflow.block_reason) OR isnotnull(tool.scope_violation) OR isnotnull(mcp.gateway.rule_id),1,0)
| eval has_control=if(isnotnull(control.status) OR isnotnull(control.pass_rate_pct),1,0)
| stats sum(has_tier1) as tier1_events sum(has_tier2) as tier2_events sum(has_workflow) as workflow_events sum(has_control) as control_events count as total
| eval tier1_pct=round(100*tier1_events/total,1), tier2_pct=round(100*tier2_events/total,1)
| eval readiness=case(tier2_pct&gt;=50 AND workflow_events&gt;0,"READY — content + workflow fields", tier1_pct&gt;=80,"PARTIAL — volume/technique fields only", 1=1,"SETUP — run Workshop First Win")
| table total tier1_pct tier2_pct workflow_events control_events readiness
| rename total AS "Sample Events", tier1_pct AS "Tier-1 Field %", tier2_pct AS "Tier-2 Field %",
    workflow_events AS "Workflow Events", control_events AS "Control Events", readiness AS "Matrix Readiness"</query>
        </search>
        <option name="drilldown">none</option>
      </table>
    </panel>
    <panel>
      <html>
        <h3>How this compares to Splunkbase apps</h3>
        <p><strong><a href="https://splunkbase.splunk.com/app/5742" target="_blank" rel="noopener noreferrer">ATT&amp;CK Heatmap (5742)</a></strong> — custom viz for Enterprise ATT&amp;CK. This dashboard uses Splunk native heatmap + matrix table for <strong>MITRE ATLAS</strong> with labeled GAP / OBSERVED / DETECTED states tied to OrchestraACME telemetry.</p>
        <p><strong><a href="https://splunkbase.splunk.com/app/8527" target="_blank" rel="noopener noreferrer">ATLAS AI Threat Detection (8527)</a></strong> — 10 generic SPL rules for production LLM logs. OrchestraACME adds <strong>45 reproducible lab playbooks</strong>, Workshop paths, kill chains, NIST attestation, and hunts that light up after you click attacks.</p>
        <p><strong>Full ATLAS catalog:</strong> Lab ships <strong>45 executable techniques</strong> across <strong>16 tactics</strong>. Official <a href="https://atlas.mitre.org/" target="_blank" rel="noopener noreferrer">MITRE ATLAS</a> is larger — expand via future STIX sync; gaps show as <code>GAP</code> until fired or enriched.</p>
      </html>
    </panel>
  </row>

  <!-- Legacy detail panels -->
  <row>
    <panel>
      <chart>
        <title>Kill-Chain Stage Distribution</title>
        <search>
          <query>`acme_genai_index` earliest=$time_range.earliest$ latest=$time_range.latest$
framework.kill_chain_stage=*
| stats count by framework.kill_chain_stage
| sort -count</query>
        </search>
        <option name="charting.chart">pie</option>
        <option name="charting.drilldown">none</option>
      </chart>
    </panel>
    <panel>
      <chart>
        <title>CVSS Distribution (observed)</title>
        <search>
          <query>`acme_genai_index` earliest=$time_range.earliest$ latest=$time_range.latest$
framework.cvss_score=*
| eval cvss_bucket=case(framework.cvss_score&gt;=9.0,"Critical 9+",framework.cvss_score&gt;=7.0,"High 7-8.9",framework.cvss_score&gt;=4.0,"Medium 4-6.9",1=1,"Low")
| stats count by cvss_bucket</query>
        </search>
        <option name="charting.chart">column</option>
        <option name="charting.drilldown">none</option>
      </chart>
    </panel>
  </row>

</dashboard>
