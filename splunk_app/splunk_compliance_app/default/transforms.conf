# =============================================================================
# ACME GenAI Compliance App — Lookup Definitions
# =============================================================================

[acme_framework_lookup]
filename = acme_framework_lookup.csv
default_match = false
match_type = WILDCARD(technique_id)
max_matches = 1

[acme_owasp_llm_lookup]
filename = acme_owasp_llm_lookup.csv
default_match = false
max_matches = 1

[acme_owasp_asi_lookup]
filename = acme_owasp_asi_lookup.csv
default_match = false
max_matches = 1

[acme_maestro_layers_lookup]
filename = acme_maestro_layers_lookup.csv
default_match = false
max_matches = 1

[acme_nist_rmf_lookup]
filename = acme_nist_rmf_lookup.csv
default_match = false
max_matches = 1

[acme_kill_chain_stages_lookup]
filename = acme_kill_chain_stages_lookup.csv
default_match = false
max_matches = 1

[acme_technique_playbooks_lookup]
filename = acme_technique_playbooks_lookup.csv
default_match = false
match_type = WILDCARD(technique_id)
max_matches = 1

# KV Store collection for behavioral baselines
[acme_agent_baselines]
type = kv
fields_list = agent_id, system_prompt_hash, model_artifact_hash, tool_schema_hash, approved_build, last_verified, baseline_cvss
